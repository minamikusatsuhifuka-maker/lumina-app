# RULES.md — xLUMINA 累積行動ルール台帳（Accumulated Behavioral Rules）

このファイルは **Claude Code 自身が読み書きする**。指示書233①で新設。

## 使い方（Claude Code 向け・厳守）

1. **作業開始前に必ずこのファイルを全文読む。**
2. 手戻り・バグ・院長の指摘・想定外が発生したら、**その便のうちに該当ルールを追記または更新する**（同一クラスの再発をゼロに近づけるため）。
3. ルールは抽象化して書く。個別の症状ではなく「次に同じ判断をするとき何を選ぶか」を書く。
4. 追記時は `npm run rules:lint` を実行し、重複・矛盾の候補が出たら**新規追加ではなく既存ルールの更新**を優先する。
5. 失敗の生ログ（事象・原因の時系列）は台帳ではなく `~/Downloads/lumina_traces/YYYYMMDD.md`（指示書233③）に書く。台帳には**抽象化した結論だけ**を置く。

## 形式

```
## R-NN: <一行のルール>
- 分類: [パーサ|E2E|デプロイ|UI|データ|コンテンツ|運用]
- 背景: 何が起きたか（1〜2行・個人情報なし）
- 検証: このルールを守れたか確認する方法
- 初出: 便番号 / 日付
```

- 番号は追記のみ（**欠番にしても再利用しない**＝トレースや過去の報告からの参照を壊さないため）
- 分類は上記7種のみ。増やすときは `scripts/rules-lint.mjs` の `CATEGORIES` も同時に更新する

---

## R-01: AI応答のJSONパースは `robustJsonParse` のみを使い、ローカル再実装をしない
- 分類: パーサ
- 背景: `JSON.parse` 直呼び・独自の正規表現抽出が各所に散り、コードフェンス付きや末尾欠けの応答で個別に落ちていた。
- 検証: `grep -rn "JSON.parse(" src/app/api src/lib` で AI応答を渡している箇所がゼロであること。
- 初出: 205 / 2026-08-05

## R-02: Anthropic応答は `content[0]` を固定参照せず、`type==='text'` を全連結する（`extractAnthropicText`）
- 分類: パーサ
- 背景: thinking を返すモデルでは `content[0]` が thinking ブロックになり、本文が空文字になって「生成失敗」に見えた。
- 検証: `grep -rn "content\[0\]" src` が0件。Anthropic呼び出しは全て `extractAnthropicText` 経由。
- 初出: 209 / 2026-08-05

## R-03: thinking系モデルの `max_tokens` は `Math.max(maxTokens, 2048)` でガードする
- 分類: パーサ
- 背景: 600トークン指定の呼び出しが thinking で枠を使い切り、本文0字が全滅で返った。
- 検証: 呼び出し側の maxTokens 定数が 2048 未満のまま渡っていないこと（`ai-client.ts` / 各routeのgrep）。
- 初出: 209 / 2026-08-05

## R-04: Gemini の `responseMimeType:'application/json'` 併用時は思考枠を見込んで `maxOutputTokens` を拡大する
- 分類: パーサ
- 背景: Gemini 3.x は思考が既定ONで、枠固定のJSON抽出が思考に食われて途中で切れた（2048→4096へ拡大で解消）。
  241追記: **思考を0にする手段は当てにしない**。3.6 で使えた `thinkingLevel:'minimal'` は
  3.7 では400（非対応）、`thinkingBudget:0` は 3.7 で thoughts が0にならず 3.6 では400。
  モデルが変われば「抑制」は消えるが、枠の上乗せはどのモデルでも効く。
- 検証: `responseMimeType` を使う箇所の `maxOutputTokens` が 4096 以上であること。
  小枠（〜1000）の Gemini 呼び出しは `geminiMaxTokens()` で思考分を上乗せしていること
  （thinking の抑制指定だけを根拠に枠を切らない）。
- 初出: 178 / 2026-08-04（241で更新）

## R-33: 外部APIは必ず `response.ok` を確認し、エラーを下流の別症状に化けさせない
- 分類: データ
- 背景: Anthropicが400「利用上限に達しました」を返していたのに `response.ok` を見ず
  `data.content`(=undefined) を渡したため、画面には「JSONパース失敗」と表示され、
  課金の問題がコードの不具合として3日間誤診断された。
- 検証: `lib/anthropic-error.ts` の `assertAnthropicOk` / `describeAnthropicError` を通すこと。
  `tests/e2e/unit.spec.ts` U1-U2 が課金上限・レート制限・認証・過負荷を区別できること。
- 初出: 234 / 2026-08-08

## R-34: 新規画面を作ったら、関連する既存画面から必ず導線を1本張る
- 分類: UI
- 背景: 228dで新設した⚡おまかせ投稿がサイドバーにしか無く、関連画面（✍note記事生成）から
  辿れなかったため「ボタンが存在しない」として機能停止と同等に扱われた。
- 検証: 新規画面のE2Eに、関連画面からのリンク実在（href一致）を1件含めること（C25が雛形）。
- 初出: 234 / 2026-08-08

## R-35: 機能削除便（孤児API削除等）の後は、削除対象に依存していた経路の実動作を必ず確認する
- 分類: デプロイ
- 背景: 234の障害調査で「スタジオ撤去(125cea1)が outline 経路を巻き込んだのでは」という
  仮説の検証に時間を要した。削除便の直後に依存経路を1回通していれば即座に切り分けられた。
- 検証: 削除便の完了報告に「削除対象を参照していた経路」と、その実行結果を明記すること。
- 初出: 234 / 2026-08-08

## R-36: AI経路の成否を検証しないE2Eは「動いている」の証拠にならない
- 分類: E2E
- 背景: 目次生成が全目的で失敗していた間も、既定スイート27件は全通過していた。
  既定スイートは画面が開くかしか見ておらず、AI呼び出しの成否を一度も検証していなかった。
- 検証: AI経路の成否は `npm run test:e2e:gen`（@gen・実課金）で確認する。
  プロバイダ・モデル・課金設定を変更した後は必ずこちらを実行する。
- 初出: 234 / 2026-08-08

## R-37: 外部APIの上限・障害時は共通層でフォールバックし、切り替えをUIで明示する
- 分類: データ
- 背景: Anthropicの上限到達で全Claude機能が停止した。各ルートに個別のリトライを書くと
  抜けが出るため、切替は `lib/ai-fallback.ts` 1本に集約する。
  ただし無言で別モデルに変わると品質差の原因が追えなくなるため、必ず画面に出す。
- 検証: 生成系APIの戻り値に provider が含まれ、gemini のとき画面に「✨ …で生成」が出ること。
  切替対象は上限・混雑のみで、認証エラーは切り替わらないこと（`tests/e2e/unit.spec.ts` U5・U6）。
- 初出: 235 / 2026-08-08

## R-39: AI等の付加情報の生成失敗が、ユーザーデータの保存自体を失敗させてはならない
- 分類: データ
- 背景: 保存の前提にAI（タイトル・タグ・分類）を置くと、AIが落ちた日に保存ができなくなる。
  付加情報は劣化して構わないが、本体の保存を失敗させるのは**データ喪失**という別種の害。
  fail-closed（偽の成功を作らない）とは対象が違う——守るべきは「本体が確実に残ること」。
  249追記: 同じ原則は**読み取り側**にも要る。一覧APIに新機能の付加情報（所属フォルダ等）を
  足すとき、本体の SELECT を書き換えて join すると、その新機能の不具合が一覧そのものを
  落とす。付加情報は別クエリにして try/catch で隔離し、失敗しても空で返す。
  一方、**絞り込み条件**は本体クエリに入れるしかない（別クエリでは件数・ページングが合わない）ので、
  そこは前提テーブルを `ensure*` で確実に用意してから使う。
- 検証: 保存APIがAI呼び出しを await していないこと。AI由来の付加情報は
  fire-and-forget かクライアント側のフォールバック付きであること。
  一覧APIでは、新機能の付加情報が本体クエリと別に取得され、失敗時も items が返ること。
- 初出: 237 / 2026-08-08（249で更新）

## R-40: DBへ渡すユーザー文字列は `sanitizeForDb` を通す（NUL・孤立サロゲート）
- 分類: データ
- 背景: 237で、AI出力に混じった NUL文字（U+0000）と孤立サロゲートで
  PostgreSQL の INSERT が例外になり、ディープリサーチのレポートが1件も保存できなかった。
  不可視文字のため画面では気づけず、原因の切り分けに時間を要した。
- 検証: `grep -rn "INSERT INTO" src/app/api` のユーザー文字列カラムが
  `sanitizeForDb(...)` を通っていること。`tests/e2e/unit.spec.ts` U10。
- 初出: 237 / 2026-08-08

## R-41: 共通層を新設したら、通っていない経路を数えるだけでなく、**検出を機械化してビルドで落とす**
- 分類: 運用
- 背景: 235でAIフォールバックを共通層に作った際、直接fetchしている77ルートが残っていた。
  「共通層を作った」だけでは既存経路は移らない。237で「数えて報告する」ルールにしたが、
  数は109本に増え、241の利用上限で全滅した——**人が数える運用では、新しいルートが
  足されるたびに元へ戻る**。242で全数を接続し、検出を `scripts/check-anthropic-direct.mjs`
  として prebuild に組み込んだ（違反があればビルドが落ちる）。
  併せて「表示機構も同じ罠にかかる」ことが判明した: 235で作った `FALLBACK_BADGE` は
  定義されただけで**どの画面からも参照されておらず**、切替は無言のままだった。
- 検証: 共通層を作った便では (1) 接続済み◯件／未接続◯件を報告する
  (2) 未接続を検出するスクリプトを用意し、ビルドまたはlintで**失敗**させる
  （違反ありでexit 1・違反なしでexit 0を実測する） (3) 共通層が返す情報を
  実際に使っている画面が1つ以上あることを確認する（定義しただけで終わらせない）。
- 初出: 237 / 2026-08-08（242で更新）

## R-05: 失敗時に偽の成功を返さない・DBに書かない（fail-closed）
- 分類: データ
- 背景: パース失敗を「提案ゼロ」として保存し、成功したように見える壊れた状態がDBに残った。
- 検証: 各routeの catch が 500/4xx を返すこと。`success: true` を返す経路が必ず実データを伴うこと。
- 初出: 224 / 2026-08-06

## R-06: 偽の既定値で catch を握りつぶさない
- 分類: データ
- 背景: `catch { return [] }` 系が失敗を空データに変換し、原因追跡ができなくなった。
- 検証: catch 内で既定値を返すのは「表示上の劣化が許される付随処理」に限り、コメントで理由を明記していること。
- 初出: 205 / 2026-08-05

## R-07: 複数行の書き込みは部分書き込みが構造的に起きない形にする（`sql.transaction`）
- 分類: データ
- 背景: 本＋章の多段INSERTが途中で失敗し、章のない本が残留した。
- 検証: 2件以上のINSERT/UPDATEを伴う経路が transaction か、失敗時の明示的ロールバック（DELETE）を持つこと。
- 初出: 222 / 2026-08-06

## R-08: `book_meta` 等のJSONカラムは丸ごと置換せず `jsonb_set` でマージする
- 分類: データ
- 背景: `book_meta` を丸ごと上書きした結果、同居する `summaries` / `proofread` が消えた。
- 検証: `grep -rn "book_meta = " src/app/api` の全ヒットが `jsonb_set(COALESCE(...))` 形式であること。
- 初出: 227 / 2026-08-07

## R-09: neon の jsonb パスに数値をそのまま渡さない（パスは `text[]`、値は `JSON.stringify` して `::jsonb`）
- 分類: データ
- 背景: 章IDや配列indexを数値のまま埋め込み、Postgres の演算子型解決に失敗して500になった。
- 検証: `jsonb_set` 呼び出しのパスが `${[...].map(String)}::text[]`、値が `${JSON.stringify(v)}::jsonb` であること。
- 初出: 227 / 2026-08-07

## R-10: スキーマ変更は `ensureXxx()` の `ADD COLUMN IF NOT EXISTS` で冪等にする（手動SQLを前提にしない）
- 分類: データ
- 背景: マイグレーションフレームワークが無いため、手動SQL前提の機能は本番で未実行のまま壊れる。
  252追記: 列追加だけでなく**制約の付け替えも冪等に書ける**——
  `DROP CONSTRAINT IF EXISTS` → `ALTER COLUMN ... DROP NOT NULL` → `CREATE UNIQUE INDEX IF NOT EXISTS`
  の順に並べれば、既存の主キーを別の組み合わせへ移し替えても再実行で壊れない。
  値の移行（`UPDATE ... WHERE <新列> IS NULL`）も同じ関数の中に置き、条件で二重適用を防ぐ。
- 検証: 新テーブル・新カラムを使う機能に対応する `ensure*` 関数があり、初回アクセスで自動作成されること。
  既存テーブルの構造を変える便では、**本番DBに対して同じDDLを2回流して2回目も成功する**ことを
  デプロイ前に実測する（移行が片道で壊れないことの確認）。
- 初出: 145 / 2026-08-03（252で更新）

## R-11: 本番URL向けE2Eのため、新画面のスモークは push 前には通らない（push後に新基準で再検証する）
- 分類: E2E
- 背景: push前に新画面のテストを走らせて「失敗」と誤認した。テスト対象は本番デプロイ済みURL。
- 検証: push前は既存N件が全通過、push後に新画面を加えた新基準（N+α件）で再実行して報告する。
- 初出: 196 / 2026-08-05

## R-12: E2Eの前提（遮蔽要素・非同期の状態復元）は必ず固定してから本題を判定する
- 分類: E2E
- 背景: オンボーディングモーダルがクリック対象を覆い、flaky の真因になっていた。
  248追記: 同じ「前提が成立していない」型のflakyをもう一つ踏んだ——自動下書き
  （155-157・`feature_result_drafts`）はマウント後に**非同期で**入力欄を埋めるため、
  前回実行の下書きが復元されると「未入力の状態で実行キーを押す」検証が作れず、
  未入力のはずが生成APIが1回走った。テストが自分で作れない前提（サーバ由来の復元）は
  モックで固定する。さらに固定した結果、**復元が隠していた別の取りこぼしが表面化した**——
  `reload({waitUntil:'domcontentloaded'})` 直後に fill/click していたためハイドレーション前で
  入力がstateに入らず、実行が始まっていなかった（復元が結果を表示していたので、
  テストは「生成できた」と誤認して緑になっていた）。**緑のテストが緑である理由**まで疑う。
- 検証: 遮蔽要素は `tests/e2e/auth.setup.ts` の storageState にフラグを焼く。
  非同期に状態を書き込むAPI（下書き復元等）は `page.route` で空を返して固定する
  （`stubFeatureDrafts`）。操作の前にハイドレーション完了を待つ（`waitForRunReady`＝
  クライアントeffectでしか出ないキー併記を待つ）。そのうえで**前提そのものを先に
  assert する**（例:「未入力」の検証前に `toHaveValue('')`）。
  254追記: **248で直したはずの型を再発させた**——`reload({waitUntil:'domcontentloaded'})`
  直後に fill したため入力が state に入らず、「入力がある」前提が崩れたまま操作した。
  そのとき機能側は正しく「入力が空なので何もしない」経路に落ちており、テストだけが
  「案内が出ない」と落ちた。**新しいテストを書くときは、既存テストがなぜ
  `waitForRunReady` を呼んでいるのかを見てから写す**（形だけ真似ると待ちが抜ける）。
  249追記: 3つ目の型——**一覧がページングされる画面で、既存のシード記事を掴もうとした**。
  シードは検証中に他テストが作る記事へ押し出され、1ページ目（30件）から外れて
  クリック対象が現れず、テストがタイムアウトする。しかも失敗は finally の後片付けが
  「context closed」で落ちた形で出るため、真因が後片付け側に見える。
  DESC順の一覧を触るテストは、**対象を検証の直前に自分で作って**先頭に来ることを保証する。
- 検証: 遮蔽要素は storageState、非同期の状態復元は `page.route`、
  一覧の対象は当該テスト内で作成（他テストのシードに依存しない）。
  いずれも同一テストを3回連続で通せること。
- 初出: 232 / 2026-08-07（248・249で更新）

## R-32: E2Eで未認証を検証するときは `storageState: { cookies: [], origins: [] }` を明示する
- 分類: E2E
- 背景: `request.newContext({ baseURL })` は playwright.config の `use.storageState` を引き継ぐため、
  認証済みで叩いてしまい「401を検証したつもりが404で落ちる」状態になった。
- 検証: 未認証テストが401を返すこと。同じリクエストを `curl` で叩いた結果と一致すること。
- 初出: 233 / 2026-08-08

## R-13: push 範囲は毎回 `git log --oneline origin/main -3` で確認し、報告に明示する
- 分類: デプロイ
- 背景: 想定外のコミットが同梱されたことがあり、報告のコミットハッシュと実際がずれた。
- 検証: 完了報告に origin/main の最新3件が引用されていること。
- 初出: 228 / 2026-08-07

## R-14: ビルドN値は `grep -cE "^[├└┌].*(○|ƒ)"` に統一し、増減は内訳を報告する
- 分類: デプロイ
- 背景: 数え方が便ごとにぶれ、N値の増減が比較できなかった。
- 検証: 報告のN値がこのコマンドの出力と一致し、前便との差分が内訳付きで書かれていること。
- 初出: 225 / 2026-08-06

## R-15: `git add -A` / `git add .` を使わず、変更ファイルを明示指定する
- 分類: デプロイ
- 背景: 意図しない一時ファイル・テスト成果物が混入した。
- 検証: コミット前に `git status --short` を確認し、`git add` の引数が個別ファイル名であること。
- 初出: 運用規約 / 2026-08-01

## R-16: Vercel デプロイに `--force` を付けない
- 分類: デプロイ
- 背景: キャッシュ破棄の再ビルドが無駄に長く、原因の切り分けも濁る。
- 検証: `npx vercel --prod --yes --cwd <PATH>` の形であること。
- 初出: 運用規約 / 2026-08-01

## R-46: 全体スケーリングは既存の書き方に合わせて選ぶ（px中心のUIでは rem 化ではなく root の zoom）
- 分類: UI
- 背景: 240で全体の文字サイズ切替を作る際、rem基準への書き換えは数百箇所のインラインstyleを
  触ることになり現実的でなかった。`documentElement` の CSS zoom なら px指定ごと拡大でき、
  createPortal で body 直下に出るモーダルにも効く（fixed要素が正確に倍率どおり拡大することを実測）。
- 検証: 各段階で「横方向のはみ出しが0」「fixed要素が倍率どおりの寸法になる」ことを
  ヘッドレスで機械判定する（目視だけで判断しない）。既定値では zoom を当てない。
- 初出: 240 / 2026-08-08

## R-47: モデルIDの変更は「実在確認（一覧API＋疎通1回）」を経てから行う。存在しないIDへの一括置換は全機能停止を招く
- 分類: デプロイ
- 背景: 241でGemini 3.6→3.7へ全経路を切り替える際、Claude側の知識では 3.7 の実在を確認できなかった。
  存在しないIDに一括置換すれば、上限中のClaudeに加えGeminiも落ち、全AI機能が同時停止する。
  実際に確認したところ 3.7 は実在した一方で、**同世代でもパラメータの受理値は変わっていた**
  （`thinkingLevel:'minimal'` が400）。IDの実在確認と、使っているパラメータの受理確認は別物。
- 検証: 切替便では (1) `GET /v1beta/models` に新IDが載ること (2) `generateContent` の疎通が200
  (3) **コードで実際に送っている generationConfig の全パターン**を新IDで1回ずつ叩き、
  400が出ないこと——の3点を実測してから置換する。呼べない場合は切り替えず報告して止まる。
  244追記: **生成が課金上限で止まっていても、モデル一覧APIなら実在確認だけはできる**。
  Anthropicで実測: `GET /v1/models/{id}` は上限中でも200を返し、存在しないIDは404。
  一方 `POST /v1/messages` は上限チェックが先に走るため、**存在しないIDでも「上限」エラー**になり
  IDの正否を判別できない。上限中は一覧APIで実在を確認し、実生成の検証は上限解除後に回す。
- 初出: 241 / 2026-08-09（244で更新）

## R-48: 画面に固定表示する要素は既定オフにし、位置は各自の直書きでなく共通の段組みで決める
- 分類: UI
- 背景: 243の追従ボタン（📖📝💬）は常時表示で、本文や操作ボタンに重なっていた。
  さらに縦位置を各コンポーネントが `bottom: 24 / 80 / 136` と直書きしていたため、
  1つ消すと空席ができ、足すと重なる——**表示の可否と位置が別々の場所で決まっていた**のが原因。
  常時出したい要素ほど、出さない選択を既定にしておく方が安全（必要な人が設定で入れる）。
- 検証: 追従・固定要素は (1) 既定が非表示 (2) 位置は共通のフック（`useFloatingSlot`）から取り、
  コンポーネント側に座標を直書きしない (3) 表示中の要素だけを詰めて並べる
  (4) 出入りする要素（スクロール連動など）は列の**端**に置き、他の要素を動かさない。
  重なりは「隣り合う要素の間隔がサイズ以上」をヘッドレスで機械判定する（目視で判断しない・R-46）。
- 初出: 243 / 2026-08-15

## R-52: キーを割り当てるときは、OSの標準キーを奪わない／破壊的操作は「確認」ではなく「Undo」で守る
- 分類: UI
- 背景: 247でクリアにキーを付けた。素直な候補ほど標準キーと衝突する——
  `⌘+Backspace` は macOS の「行頭まで削除」（テキスト入力の標準操作）、
  `Ctrl+Shift+Delete` は Windows Chrome の「閲覧履歴の削除」（preventDefault では止まらない）。
  また、クリアに確認ダイアログを挟むと「キーで速く消す」という目的自体が消える。
  速さのために足した機能を、安全策で殺さない。
  248追記: **「衝突を避けるために修飾キーを足す」は実測しないと成立しない**。247は
  `⌘+Backspace` を避けて `⌘+Shift+Backspace` にしたが、実際にキーを送って計測すると
  **両者はまったく同じ編集コマンド（行頭まで削除）を奪っていた**（Windowsの
  `Ctrl+⌫` と `Ctrl+Shift+⌫` も同じく「直前の単語を削除」）。Shiftは編集コマンドの
  選択に影響せず、増えたのは押しにくさだけだった——院長から「押しにくい」と指摘され
  248で2キーへ戻した。そして**押しやすさは要件**である。3キー同時押しは、使われない
  ＝機能を足していないのと同じになる。
- 検証: 新設キーが (1) OS/ブラウザの標準キーと衝突しないこと（Mac・Windows両方で確認する）。
  「衝突しないはず」で決めず、候補ごとに**実際にキーを送って入力欄の本文がどう変わるかを
  機械判定**し、奪う既定動作を候補間で比較してから選ぶ（ブラウザ側の割り当ては公式の
  ショートカット一覧で裏取りする）。修飾キーを足すなら、それで奪う動作が減ることを確認する
  (2) 破壊的なら確認ではなくUndoを出すこと（ボタン押下とキーで同じ経路を通す）
  (3) 既存の同一キー（NoteBundleDockの⌘Enter等）と同時に発火しないこと——
  どちらが譲るかはDOM上の印（`data-kb-select-mode` 等）で決め、暗黙の順序に頼らない。
  (4) 修飾キー1つ＋1キー＝**2キー以内**に収まること（U15で機械判定）。
  E2E C29（未入力/入力中/実行中/クリアの4パターン＋旧キー互換＋URLが変わらないこと）。
- 初出: 247 / 2026-08-21（248で更新）

## R-53: 「自動で保存する」機能は〈保存済み表示・二重保存の防止・失敗時の手動再試行〉を必ずセットで作る
- 分類: データ
- 背景: 247で生成結果の自動ストック保存を入れた。自動保存で一番困るのは画面が黙ることで、
  保存済みが見えないと手動でも押して重複し、失敗が見えないと「結果を失った」と思って
  作り直しになる。保存の成否は必ずボタン自身の状態として残す（トーストは消える）。
  発火は「本文が変わったら」ではなく「生成が完走したら」にする——ストリーミング中と
  自動下書きの復元でも本文は変わるため、前者だと重複保存になる。
- 検証: 自動保存を入れた画面で (1) 保存後にボタンが「✅ 保存済み」になる
  (2) 保存済みの間は押せない（本文が変われば解除され、直した版はまた保存できる）
  (3) 保存に失敗しても生成結果は画面に残り、「⚠️ 保存に失敗・再試行」から手動で戻せる（R-39）。
  既定ONにするなら設定画面でOFFにできること。E2E C30 / C31。
- 初出: 247 / 2026-08-21

## R-49: 同じ目的のUIが2つあったら、経路の違いで残さず1つに統合する（残す側は成果物を汚さない方）
- 分類: UI
- 背景: 245のディープリサーチには期間指定UIが2つあった。実体は**どちらもAIへの期間指示**で、
  違いは経路だけ——①トピック文字列の末尾に条件文を追記してから送る ②APIに日付を渡して
  サーバーがプロンプトに節を組み立てる。①はトピックがライブラリ保存タイトル・履歴・
  タイトル生成に使われるため、検索条件文が成果物に混入していた。②に一本化した。
- 検証: 同種UIが複数あるときは「何を汚すか」で残す側を選ぶ（ユーザーの入力値をそのまま
  保存・表示に使う経路に、機械的な条件文を足さない）。統合後、旧経路の state・import・
  ヘルパーが残っていないことを grep で確認する。
- 初出: 245 / 2026-08-15

## R-50: `<details open={式}>` のように値だけ制御して変更を受け取らないUIを作らない
- 分類: UI
- 背景: 245で `open={isCustomPeriod}` と書いたため、条件が満たされるまで常に false が
  再適用され、**ユーザーが折りたたみを開けない**状態になった（E2Eが「element is not visible」で
  タイムアウトして発覚）。制御コンポーネントの片側だけを実装した形。
- 検証: `open` / `value` / `checked` を式で渡す要素には、必ず対になる
  `onToggle` / `onChange` を付けて state へ書き戻す。開閉・入力を伴うUIは、
  E2Eで「開いて操作できる」ことまで確認する（表示の有無だけで済ませない）。
- 初出: 245 / 2026-08-15

## R-51: 既定値の変更は「否定形の分岐」まで直す（`X以外はY` を残すと既定が変わらない）
- 分類: 運用
- 背景: 244で既定AIモデルをGeminiへ変えた際、`model = 'claude'` のような素直な既定は
  grepで拾えたが、`body.model === 'gemini' ? 'gemini' : 'claude'`（＝geminiと明示されなければ
  全部Claude）という**否定形で既定を表現した分岐が7本**残っていた。これを見落とすと、
  定数を切り替えても実際の既定は変わらない——しかもビルドもテストも通るので気づけない。
- 検証: 既定値を変える便では、新しい既定値だけでなく**旧既定値そのもの**を全数grepし、
  三項演算子・`||`・`??`・switch の default に旧値が残っていないか確認する。
  「旧値と一致したときだけ旧値、それ以外は新既定」の向きに揃える。
- 初出: 244 / 2026-08-15

## R-17: Tailwind のクラス名は完全リテラルで書く（動的組み立て禁止）
- 分類: UI
- 背景: `bg-${color}-500` 形式は本番ビルドでパージされ、色が消えた。
- 検証: `grep -rnE "className=\{\`[^\`]*\\$\{" src` にクラス名の動的生成が無いこと。
- 初出: 139 / 2026-08-03

## R-45: ユーザーが「読む」画面はMarkdownを整形表示し、「編集する」画面でのみ生テキストを見せる
- 分類: UI
- 背景: ⑤の編集モーダル・テイスト変換のサンプル・⑥のプレビューが生Markdownのままで、
  `##` `**` が露出して読みづらいと院長から指摘。読む用途と直す用途を同じ表示で兼ねていたのが原因。
- 検証: 生成物を表示する箇所が `renderMarkdown` + `className="markdown-body"` を通ること。
  `whiteSpace: 'pre-wrap'` で生テキストを出すのは textarea と、位置ベースの差分表示だけに限る。
  保存形式は変更しない（表示のみ・相互変換を作らない）。
- 初出: 239 / 2026-08-08

## R-18: `###` などの Markdown 記号をアプリ内表示にそのまま出さない
- 分類: UI
- 背景: AI出力の見出し記号が画面にそのまま出て、素人っぽい表示になった。
- 検証: 表示経路が `markdown-renderer.ts` を通るか、プロンプトで `###` を禁じていること。
- 初出: FAQレイアウト / 2026-08-02

## R-19: `.page-enter` の transform 配下では `position:fixed` が効かない → モーダル等は `createPortal`
- 分類: UI
- 背景: ページ遷移アニメの transform が残留し、モーダルが画面中央でなく親要素基準に出た。
- 検証: fixed系オーバーレイが `createPortal(..., document.body)` で描かれていること。
- 初出: 189 / 2026-08-05

## R-20: 生成結果は自動保存・復元する（`feature_result_drafts`）。リロードや遷移で消さない
- 分類: UI
- 背景: 長時間の生成結果が画面遷移で消え、やり直しが発生した。
- 検証: 生成系画面が `feature-drafts.ts` 経由で下書きを保存・復元していること。
- 初出: 155-157 / 2026-08-04

## R-21: 素材にない事実を追加しない（数値・出典・研究名・固有名詞の創作禁止）
- 分類: コンテンツ
- 背景: 生成本文に素材に無い統計値・機関名が混入した。
- 検証: `lib/content-verify.ts` の素材照合で「素材にない記述」を確認する（233②）。プロンプトにも厳守事項として明記。
- 初出: 224 / 2026-08-06

## R-22: 医療広告ガイドライン（医療法・薬機法）に配慮する。効果保証・誇大・不安煽り・限定性・費用誤認を書かない
- 分類: コンテンツ
- 背景: 集客目的の表現が規制NGに寄りやすい。
- 検証: `medical-ad-check.ts`（AI判定）と `content-verify.ts` の禁止表現チェック（辞書判定）の両方で警告ゼロを確認。
- 初出: 145 / 2026-08-03

## R-23: 画像・図表の文言は編集後データのみを使う（AIによる再要約を挟まない）
- 分類: コンテンツ
- 背景: 院長が編集したまとめをAIが再要約し、画像の文言が編集前に戻った。
- 検証: 画像生成の入力が編集済みデータ（`source: 'edited'` を含む保存値）から直接渡っていること。
- 初出: 227【C】 / 2026-08-07

## R-24: ビフォーアフター枠を治療前後・効果対比に使わない
- 分類: コンテンツ
- 背景: 医療広告ガイドラインで術前術後の対比表現が制限されるため。
- 検証: ビフォーアフター系テンプレートの用途説明が「治療効果の対比ではない」ことを明示していること。
- 初出: 227【C】 / 2026-08-07

## R-25: マイ文体の学習元は院長自身が書いた文章のみ（AI生成物を混ぜない）
- 分類: コンテンツ
- 背景: ライブラリにはAI生成物しかなく、それを文体学習に使うと自己模倣ループになる。
- 検証: マイ文体の取り込み口が院長の手入力・手持ち原稿に限定されていること。
- 初出: 228c / 2026-08-07

## R-38: 本文を書き換える機能は、生成結果をサーバ側で保存せず、適用前に必ず前後差分を見せる
- 分類: コンテンツ
- 背景: テイスト変換（236）は章の全文を入れ替える。生成時点で保存してしまうと、
  院長が内容を見る前に本文が変わる。「保存しない」ことをAPIの仕様にすれば、
  UIの実装ミスがあっても無言の書き換えは起こり得ない。
- 検証: 変換系APIが `success` を返した直後に本文を再取得しても変わっていないこと
  （236の通し検証で「変換API時点で本文は無傷」を実測）。適用は別エンドポイントの明示操作のみ。
- 初出: 236 / 2026-08-08

## R-42: 出力の不具合は、まず出力器を単体で再現してから原因を決める
- 分類: デプロイ
- 背景: 238でWord出力に混入が出た際、指示書の疑いは「docxのheader/footerが本文に落ちている」
  だったが、エクスポータをNodeで単体実行して document.xml を実測したところ完全に正常で、
  真因は生成済みの本文側にあった。出力器を疑ったまま直していたら見当違いの修正になっていた。
- 検証: 出力系の不具合報告では、生成物（docx/PDF/画像）を機械的に開いて中身を数える
  （Titleスタイルの個数・見出しの出現回数など）。人間の目視だけで原因を決めない。
- 初出: 238 / 2026-08-08

## R-43: 警告色は背景とのコントラスト比 4.5:1 以上（WCAG AA）を満たす色を使う
- 分類: UI
- 背景: 検証警告に使っていた `#f59e0b` はクリーム系背景(#F5F3EF)に対して約2.2:1しかなく、
  院長が「読みにくい」と実運用で指摘。警告は読まれなければ存在しないのと同じ。
- 検証: 警告色は `#B45309`（約5.1:1）以上のコントラストを持つこと。
  新しい警告色を足すときは背景色に対する比を計算してから採用する。
  246追記: **必要な比は文字サイズで変わる**。18.66px以上かつ太字（WCAGの「大きな文字」）は
  3:1、通常サイズの文字は4.5:1。アイコンボタンの記号は前者に当たる
  （246の↑ボタンは24px/700で、塗り色に対し dark 4.32:1・light 5.50:1＝どちらも3:1を満たす）。
  塗りの上に文字を置く要素を足すときは、この比を**計算して**から採用する（見た目で決めない）。
- 初出: 238 / 2026-08-08（246で更新）

## R-44: 機械検出は「件数」より「確認すべきものが上に来るか」で設計する
- 分類: コンテンツ
- 背景: 内容検証が7章の本で39件を出したが、中身は一般的なカタカナ語ばかりで、
  本当に確認すべき固有名詞・数値が埋もれていた。検出漏れがなくても、
  読む気を失わせた時点で機能していない。
- 検証: 検出結果に優先度（🔴要確認／🟡参考）があり、既定表示が🔴のみであること。
  除外辞書を増やしたら、確認すべき語が消えていないことを同じサンプルで確認する。
- 初出: 238 / 2026-08-08

## R-26: 検証機能は表示のみ。自動修正・自動削除・自動ブロックをしない
- 分類: コンテンツ
- 背景: 誤検出で生成が止まる方が、見逃しより害が大きい。
- 検証: 検証系APIの戻り値が警告リストのみで、本文を書き換える経路が存在しないこと。
- 初出: 233 / 2026-08-08

## R-27: 指示書ファイルの再送は承認ではない（承認は院長の明示的な「承認」の返信のみ）
- 分類: 運用
- 背景: 同じ指示書の再送を承認と解釈して先に進んだことがある。
- 検証: 停止指定のある便で、承認文言の受領なしに次段へ進んでいないこと。
- 初出: 運用規約 / 2026-08-06

## R-28: リポジトリ外の成果物は `~/Downloads/` に短い半角名で置く
- 分類: 運用
- 背景: 日本語長名・Desktop配下は macOS の TCC で読めなくなることがある。
- 検証: 報告用ファイルのパスが `~/Downloads/<半角短名>.md` であること。
- 初出: 運用規約 / 2026-08-04

## R-29: 個人情報・患者情報を git・チャット・ログに残さない
- 分類: 運用
- 背景: 医療機関のリポジトリであり、混入したら取り消せない。
- 検証: コミット差分・トレースファイルに識別可能な個人情報が無いこと（トレースは識別子と事象のみ）。
- 初出: 運用規約 / 2026-08-01

## R-30: `cd` を使わない（`git -C` / `npm --prefix` / `--cwd` で対象を指定する）
- 分類: 運用
- 背景: パス解決の警告と承認待ちが発生し、自動運転が止まる。
- 検証: 実行したコマンド列に `cd ` が1件も含まれないこと。
- 初出: 運用規約 / 2026-08-01

## R-31: AI系・データ系のAPIルートは既定で認証必須にする（公開は明示的なホワイトリストのみ）
- 分類: 運用
- 背景: 未認証で叩ける生成APIが多数残り、コストと情報の両面で露出していた。
- 検証: `src/app/api` 配下の route が `auth()` または `lib/require-auth.ts` を通ること。公開例外が列挙されていること。
- 初出: 160 / 2026-08-04

## R-54: 既存の分類に別軸を足すときは、既存カラムを流用せず別テーブル＋scopeで分ける
- 分類: データ
- 背景: 249で「院長が名前を付けるフォルダ」を追加した。既存の自動カテゴリ
  （`text_analysis_saves.folder` / `context_saves.category`）は1件1つ・AIが決める軸で、
  新機能は1件に複数・人が決める軸——**性質が違う**。既存カラムを流用すると
  自動カテゴライズを1回走らせただけで院長の手作業が消える。R-49（同じ目的のUIは統合する）
  と対になるルールで、判断基準は「目的が同じか」であって「見た目が似ているか」ではない。
  併存させる場合は、画面ごとの体系の混線も同時に防ぐ（`scope` 列で分け、
  所有者検証を `INSERT ... SELECT ... WHERE EXISTS` に置いて他スコープのIDを弾く）。
  252追記: **「どのテーブルの行か」と「どのグループ体系に属するか」は別の軸**として持つ。
  249では scope 1本で両方を表していたため、「保存一覧とリサーチ保存でフォルダを共有したい」
  という後からの要望に構造が追いつかなかった。アイテム種別（`custom_folder_items.scope`）と
  体系（`custom_folders.scope`）に分け、対応表（`FOLDER_SYSTEM_OF`）を1箇所に置くと、
  共有範囲の変更が**マッピング1行**で済む。IDの型が違うテーブル（uuid と integer）を
  同じ所属表に載せるときは、`item_key text` に寄せて `id::text` で突き合わせる。
- 検証: その便の `git diff` に既存カラムへの UPDATE（`SET folder = ` / `SET category = ` 等）の
  追加が1行も無いこと。既存データが未分類として無改変で残ること。スコープ分離はE2Eで「同名フォルダが両画面に作れる」
  「相手スコープのフォルダIDを渡しても件数が増えない」まで機械判定する（C34が雛形）。
  共有体系は「片方で作ったものがもう片方に出る」「1つのグループに両方の行が混在して数えられる」
  「独立体系には出ない」の3点を判定する（C42が雛形）。
- 初出: 249 / 2026-08-21（252で更新）

## R-55: E2Eが作るデータには識別接頭辞を必ず付け、付与は**ヘルパー側で強制**する
- 分類: E2E
- 背景: 自動掃除（`cleanupE2E*`）は `[E2E]` の有無だけでテストデータを判定する。
  接頭辞の付与を呼び出し側の記述に任せていたため、書き忘れた1件が掃除対象から外れ、
  約1か月（2026-07-29〜08-21）本番DBに残り続けた。掃除ロジックが正しくても、
  **入口が緩ければ残骸は必ず出る**——判定基準を持つ側が、生成側も縛る。
  同じ理由で、作成ヘルパーを持たないテーブル（直に POST していた経路）にも
  ヘルパーを用意し、`afterAll` の掃除を全テーブル分そろえる。
- 検証: `tests/e2e/helpers.ts` の create* がすべて `withE2EPrefix()` を通していること。
  `grep -n "request.post(" tests/e2e/*.spec.ts` にテストデータの直接作成が無いこと。
  掃除は `afterAll` で全対象テーブル（保存一覧・AI参照素材・リサーチ保存・フォルダ）に
  対して走ること。実行後に本番DBで E2E アカウントの残骸が0件であることを実測する。
- 初出: 250 / 2026-08-21

## R-56: 破壊的な一括操作は「件数を明示した確認」を通す口を1本にまとめる
- 分類: UI
- 背景: 250で3画面（🗂テキスト分析・🧠AI参照素材・📚リサーチ保存）に一括削除を足した。
  削除は不可逆で Undo を持たないため、確認の文言が画面ごとにぶれると
  「どれが戻せない操作か」の学習が効かなくなる。文言は `lib/bulk-delete-confirm.ts`
  1本に集約し、必ず〈件数・対象の種類・戻せないこと〉の3点を出す。
  APIも同様に、空の `ids` は400で弾く（誤って全件を対象にする経路を作らない）。
- 検証: 一括削除を持つ画面が `confirmBulkDelete()` 経由であること
  （`grep -rn "confirm(" src` に一括削除の独自文言が無い）。
  E2Eで「キャンセルすると1件も消えない」ことまで判定する（C40が雛形）。
- 初出: 250 / 2026-08-21

## R-57: 名前を可変にするのは「導線のラベル」だけ。実体の見出しや文中の参照名は固定する
- 分類: UI
- 背景: 251でサイドバーのメニュー名を院長が変更できるようにした。ページ内の見出し(h1)まで
  追随させると、その画面を指す**他画面の案内文**（「生成時にAIへ参照させたいものは
  🧠 AI参照素材 へ」等）が旧名のまま残って食い違う。既定名は文中参照が多く
  （「AI参照素材」だけで29ファイル、「ディープリサーチ」は48ファイル）、
  追随させるなら全参照を可変にする必要があり、漏れが必ず出る。さらにE2Eが見出し名で
  到達判定をしているため、見出しが可変になると機械判定の基準そのものが壊れる。
  サイドバーは「自分の導線のラベル」なので、そこだけ変えても他の文と矛盾しない。
  対応が分からなくならないよう、変更した項目には既定名をツールチップで併記する。
- 検証: リネーム機能を入れる便では、その名前が**文中に何ファイル現れるか**を数えてから
  範囲を決める（`grep -rl "<既定名>" src | wc -l`）。可変にした範囲の外（h1・案内文・URL）が
  既定のままであることをE2Eで判定する（C41が雛形）。識別子（href・内部キー）は絶対に変えない。
- 初出: 251 / 2026-08-21

## R-58: 「共有する」と決めたら、開いたときに中身が全部見えるまでを1セットで実装する
- 分類: UI
- 背景: 252で保存一覧とリサーチ保存のフォルダ一覧を共有し、件数も合算で出した。
  しかし絞り込みは各画面が自分のテーブルだけを見ていたため、**バッジは4件なのに開くと2件**
  という状態になった。しかも当時のE2E（C42）は「絞り込みは各画面のアイテムだけ返す」ことを
  **正しい仕様として機械判定していた**——テストが緑でも、院長の意図とは食い違っていた。
  共有とは「同じ入れ物を使う」ことであり、入れ物を開いたら中身が全部見えるのが自然。
  片方だけ実装すると、**合算した件数が嘘になる**（見えない分を数えている）。
- 検証: グルーピングを画面間で共有する便では、(1) 件数の集計範囲 (2) 絞り込みの取得範囲
  (3) 一覧に出る範囲 の3つが同じ母集団を指しているかを確認する。ズレるなら、件数の側を
  自画面に狭めるか、取得の側を広げるかを**先に決めてから**作る。
  E2Eは「バッジの件数」と「実際に表示される件数」が一致することを判定する（C45が雛形）。
  混在表示にしたときは、アイテムの出自を画面に出し、操作は種類ごとに実際に叩けるAPIへ
  振り分ける（使えない操作は最初から出さない＝押して失敗する口を作らない）。
  併せて、**表示の切り替え方を変えたら、旧UIの形に依存していた既存テストを全部洗う**。
  253では「絞り込み後にカードが1件」を数えていたC36・C43が、横断ビューへの差し替えで
  一斉に落ちた（指示書はC42の追随だけを指していたが、同じ形の判定は他にもあった）。
  同じ要素セレクタを使っているテストを grep して、まとめて新しい形へ寄せる。
- 初出: 253 / 2026-08-22

## R-59: ブラウザ権限に依存する機能は、拒否されたときの経路まで作って初めて完成
- 分類: UI
- 背景: 254の「クリアして貼付」は `navigator.clipboard.readText()` に権限が要り、
  拒否されると読めない。実測（Chromium）では権限ありで成功・権限なしで NotAllowedError。
  権限が無いときに「何も起きない」で終わらせると、押しても反応しないボタンになる。
  **読み取りを先に試し、結果が分かってから入力を触る**——読めたら置き換え、読めなければ
  クリアしてフォーカスまで済ませ「⌘Vで貼り付けてください」と伝える（半分は自動で進む）。
  **270で後半を撤回した**（→R-76）。「読めなければクリアだけ済ませる」は、拒否が例外的な
  デスクトップでは親切に見えたが、**iOSでは確認をキャンセルするたびに通る道**で、
  長文を書いた直後の誤操作が全消しになる。順序が正しくても、**破壊的操作を先に確定させて
  しまえば意味がない**。読めなかったときは入力に触らず、案内だけ出す。
  254では併せて「キー押下でブラウザ標準の貼り付けを通す」案も検討したが、実測すると
  標準の貼り付けを起こせるのは ⌘V だけで、そのキーは奪ってはいけない＝案として成立しなかった。
  **代替案は机上で選ばず、実際にキーを送って何が起きるかを見てから決める**（248と同じ）。
- 検証: 権限あり／権限なし／空／実行中の4パターンをE2Eで判定する（C46・C47が雛形）。
  権限なしのテストは、権限を与えないブラウザコンテキストを別に作って再現する。
  どの経路でも元の内容が失われないこと（Undoで戻せること）まで確認する。
- 初出: 254 / 2026-08-22

## R-60: キー本数の上限のような「形で縛るルール」に例外を作るときは、理由をテストに書いてから足す
- 分類: 運用
- 背景: 248で「生成・実行画面のキーは2キーまで」を単体テスト（U15）で機械化した。
  254で ⌘⇧V（3キー）を足したところ、意図どおりU15が落ちた——**縛りが機能した**。
  ここで上限を黙って緩めると、次からは何本でも足せてしまう。項目ごとに上限と理由を
  書いた表をテスト内に置き、新しい項目は必ずそこへ登録してから足す形にした。
  254が3キーを許した理由: Mac/Windowsとも「書式なしで貼り付け」の標準キーが ⌘⇧V で、
  それを踏襲した方が覚えやすい。⌘⇧⌫ が押しにくかったのは右手が窮屈になるからで、
  ⌘⇧V は左手だけで押せる＝同じ「3キー」でも押しやすさの質が違う。
  270追記: 📝テキスト分析を全端末3ボタンにした便でも、**キーの割り当ては1つも増やしていない**
  ——増えたのは押せるボタンだけで、⌘⇧V は254で登録済みの例外のまま。
  例外表（U15の MAX_KEYS）は「変更が無かったこと」も含めて便ごとに見直し、
  併せて一覧の補足文（note）が実装の結末と合っているかまで確認する
  （270では「読めないときはクリアだけ行う」が実装と食い違う嘘の案内になっていた）。
- 検証: 制約を機械化したテストが落ちたら、まず「制約が正しく効いた」と解釈する。
  緩めるときは上限値だけを書き換えず、例外の理由をテスト内のコメントに残す。
  ショートカットの**結末**を変えた便では、SHORTCUT_SECTIONS の note を単体テストで実装と突き合わせる（U21）。
- 初出: 254 / 2026-08-22

## R-61: OSやブラウザが強制する確認は消せない。**確認が既に含まれている操作に機能を乗せる**
- 分類: UI
- 背景: 254の「クリアして貼付」はiPhoneで2タップになった。iOS Safari は
  `clipboard.readText()` に必ず確認ポップアップを出す（Appleのクリップボード保護で、
  Webアプリ側からは無効化できない）。ここで「確認を消す方法」を探しても出口はない。
  255で出口になったのは**発想の向き**だった——ユーザーが普段する「長押し→ペースト」には
  確認が最初から含まれている。その `paste` イベントに乗れば、追加のタップはゼロになる。
  実測: `paste` イベント内の `clipboardData.getData('text/plain')` は
  **Chromium・WebKit（＝iOS Safari と同じエンジン）とも権限もプロンプトも無しに読める**。
  「権限が要るAPI」と「イベントで渡されるデータ」は別物で、後者には確認が要らない。
  258追記: **255はここまでで止まっていて、院長には一度も届いていなかった**。
  逃げ道（paste イベント経路）は作ったのに (1) 既定OFFのままで (2) 多段確認になる
  旧入口（📋クリアして貼付ボタン）を全端末に残していたため、iPhoneでは
  「許可しますか→許可→ペースト」を踏み続けていた。**逃げ道は、それが要る端末では
  既定で開いていないと使われない**し、**旧入口が残っていればそちらが使われる**。
  代替経路を用意する便では、対象端末での既定と旧入口の撤去まで込みで1セットにする。
  ただし本人が明示的に設定した値は端末判定で覆さない（既定を決めるのは未設定のときだけ）。
  入口を消す端末には、代わりの操作を入力欄のそばに必ず案内する（消しただけだと機能喪失に見える）。
  259追記: **258の「既定ONにする」は半分間違いだった**ので取り下げた。
  逃げ道（本文欄を長押し→ペースト）は、**文章の途中を長押しすると iOS の選択メニュー
  （選択／すべてを選択／ペースト…）が出て、狙ったペーストに辿り着けない**——
  経路として存在しても、迷わず使えなければ主経路にはならない。
  順序は〈逃げ道が迷わず使えるか確かめる〉→〈使えるものを主経路にする〉→〈既定を決める〉。
  使えないものを既定ONにして押し込まない。そして**主経路でなくなったものは既定OFFに戻す**
  （残しておくと、追記したいだけの操作が黙って全消しになる——頻度が低くても損失が大きい方に
  既定を寄せない）。259の答えは「見た目はボタン・実体は**空の**編集可能領域」だったが、
  **260でこれも外した**——編集可能な欄はタップでキーボードが立ち上がり、欄の位置がずれて
  押し直しになる。258（選択メニュー）・259（キーボード）と、外した原因はどちらも
  **ヘッドレスでは観測できないOSのUI**だった。3回目は賭けず、
  **編集可能な要素を一切使わない形**（📋 ペーストボタン＝`readText()`）に一本化した。
  確認ポップアップは残るが、それは測って「回避手段が無い」と確かめた**既知の限界**であって
  賭けではない。**観測できないUIに依存する案と、既知の限界を受け入れる案が並んだら後者を採る。**
- 検証: 権限プロンプトが絡む機能では、同じ結果を**ユーザーの既存操作のイベント**から
  得られないかを先に実測する（`paste` / `drop` / `change` など）。
  乗せた結果ユーザーの操作の意味が変わる場合は、既定OFFの設定にして選べるようにし、
  Undoを必ず付ける。E2Eは設定OFF（従来どおり）とON（新しい意味）の両方を判定する。
  258追記: 端末で挙動を分ける便では (1) 既定の決まり方を純関数に切り出して
  「未設定×端末」「保存値あり×端末」の全組み合わせを単体テストで固定する（U25）
  (2) 対象端末の**実エンジン**でE2Eを回す（iOSはWebKit＋hasTouch/isMobileの
  コンテキストを起こす。UA判定をしていないのでこれで実機と同じ分岐に入る・C52）
  (3) 旧入口がその端末に**出ていない**ことまで判定する（代替を足しただけで終わらせない）。
  端末判定は `lib/pointer-device.ts` の1本だけを使う（同じ問いを各所のmatchMediaに書かない）。
- 初出: 255 / 2026-08-22（258で更新）

## R-62: 補助表示（ふきだし・プレビュー）は「操作を妨げない」を先に設計する
- 分類: UI
- 背景: 256でカードのホバープレビューを足した。読むための補助表示は、置き方を誤ると
  本来の操作を奪う——ふきだしがボタンに重なればクリックが通らず、ホバーの無い端末では
  タップのたびに邪魔が出る。先に決めたのは見た目ではなく**通さない条件**だった:
  (1) `pointer-events: none` で下のボタン（分類・削除・選択）を絶対に覆わない
  (2) `(hover: hover) and (pointer: fine)` のときだけ動かす（UA判定はしない）
  (3) スクロール・リサイズ・タブ切替で消す（前の位置に取り残さない）
  (4) 遅延を置く（一覧を眺めて動かすだけで次々出ると煩わしい）
  (5) 設定でオフにできる
  取得の面でも、**本文を持っている画面はそのまま使い、持っていない画面は既存の
  キャッシュ付き取得を通す**＝ホバーのたびにAPIを叩かない。
  257追記: 256は「妨げない」は満たしたが、**どこに出すか**を落とした。
  位置をカーソル座標基準にすると、(a) カードのどこから入ったかで出る場所が変わり
  (b) 画面端の反転が**カーソルを基準に箱ごと**動くため、箱の寸法ぶん飛ぶ。
  本番実測でカードが y=636 なのにプレビューは y=370（266px 上）に出ていた——
  ホバー対象と無関係な場所に見えるので「対応が分からない」となる。
  補助表示の位置は**対象要素の矩形**を基準に置き、対象の隣（右→左→下→上）に
  貼り付けて、はみ出しは**箱を動かすのではなく画面内へクランプする**。
  併せて、遅延の中に取得を隠すのではなく**取得を遅延より前に始める**（先読み）と、
  遅延を縮めなくても体感が変わる（「読み込み中…」が出なくなる）。
- 検証: 補助表示を足す便では、E2Eで「表示される」だけでなく
  **「カーソルが外れたら消える」「設定OFFで出ない」「タッチ端末で出ない」**を判定する
  （C49・C50が雛形）。本文取得が増えていないことは、既存のキャッシュ関数を
  経由していることをコードで確認する。
  なお**ホバーを検証するテストは、検索で絞った直後のカードを掴まない**——
  デバウンス後の再描画とホバーが競合して不安定になる（256で実際に踏んだ。
  機能側は単体スクリプトの実測で正常だと確かめたうえで、テストの掴み方を直した）。
  作成直後の行が先頭に来る性質を使うか、一覧が落ち着いてから当て直す。
  257追記: 位置は**目視で判断しない**（R-46/R-48と同じ）。配置計算を純関数に切り出して
  四隅・中央・グリッド各列・横長カードを単体テストで機械判定し（U24）、
  E2Eでは**ホバー対象そのものの矩形**（`data-hover-card`）とプレビューの矩形の
  最短距離を測る（C49）。お気に入りボタン等の**子要素**を対象と取り違えると
  判定が意味を失うので、ホバーを張った要素に印を付けてそれを使う。
- 初出: 256 / 2026-08-22（257で更新）

## R-63: 畳んで隠すUIは、隠した側の状態が閉じたままで分かるようにする
- 分類: UI
- 背景: 258で分析タイプの選択肢を「よく使う2つ＋その他（折りたたみ）」に分けた。
  すっきりさせる目的で畳むのだから既定は閉じるが、畳んだ側にチェックが残ったまま
  閉じると、**実行ボタンには「3件を分析」と出るのに画面には2つしか見えない**——
  ユーザーは自分が何を選んでいるか説明できなくなる。隠していいのは選択肢であって、
  選択の結果ではない。開閉状態を保存するかは別の判断で、「すっきり」が要望なら
  保存しない方が意図に合う（前回開いたままだと元の状態に戻る）。
  分け方の正本は1箇所に置き、**常時表示する側を列挙して残りは自動的に畳む側へ回す**
  ——こう書けば項目が増えても畳む側の書き足しが要らない（取りこぼしが構造的に出ない）。
- 検証: 折りたたみを入れる便では、E2Eで (1) 既定が閉じている (2) 開けば操作できる
  (3) **畳んだ側を選んで閉じても、選択中であることと選ばれた名前が見える**
  (4) 見えている選択数と実行ボタンの件数が一致する ——の4点を判定する（C51が雛形）。
  分割は単体テストで「両側の和が全件と一致・重複なし・畳む側が空でない」を固定する（U26）。
- 初出: 258 / 2026-08-23

## R-64: ブラウザの可否は**対象エンジン**で測る。Chromiumの結果でWebKit（iOS）を語らない
- 分類: E2E
- 背景: 259で院長案「ボタンを長押ししたら貼り付け完了」の可否を実測した。
  ふつうの `<button>` に貼り付けを送ると **Chromium では paste が1回届き、WebKit では0回**
  ——Chromiumだけで測っていたら「実現できます」と正反対の報告をしていた。
  iOS Safari は WebKit なので、iOS向けの判断は WebKit の結果だけを根拠にする。
  同時に分かったのは「**paste は編集可能な領域にしか配送されない**」という原則で、
  同じボタンに `contenteditable` を付けると WebKit でも届いた＝院長案は
  「見た目はボタン・実体は編集可能な空欄」にすれば成立する。
  可否は「できない」で終わらせず、**成立する最小の形**まで探してから報告する。
- 検証: 端末・エンジン依存の機能を判断する便では、`webkit.launch()` で対象エンジンを起こし、
  候補ごとに実際の操作を送って結果を数える（`paste` の発火回数・`readText()` の例外名・
  `execCommand` の戻り値）。採用案だけでなく**不採用案の実測値も報告に載せる**
  （「できるはずなのにやっていない」という誤解を残さない）。
  なお**ネイティブの確認ポップアップの回数は Playwright では測れない**（OS/ブラウザのUIで
  ページからは観測できない）。測れないものは測れたと書かず、
  「確認を回避する手段が無いこと」（`execCommand('paste')` が false・`readText()` が
  権限必須）をもって説明する。
  260追記: **「測ったら成立した」と「実機で使える」は別**。260では readonly / inputmode="none" の
  貼り付け欄はどちらも paste が届いたが、それはヘッドレスが**ハードウェアキーボードの ⌘V**を
  送っているからで、実機のiPhoneにその手段は無い（貼り付けを起こせるのは長押しメニューだけ＝
  観測できないUI）。**送っている操作が実機に存在するか**まで確かめ、しないなら
  「測れた範囲」と「実機で成立する保証が無い範囲」を分けて報告する。
- 初出: 259 / 2026-08-24（260で更新）

## R-65: 新規スモークの判定対象は「一意で・可視な」要素かを先に確かめてから書く
- 分類: E2E
- 背景: 261で新画面のスモークを2回続けて同型で落とした。(a) ページ内導線を
  `getByRole('link', {name})` で判定したら、**サイドバーに同名のメニューリンク**があり
  strict modeで2件に解決された（新メニューを足した便は、その名前が必ずサイドバーにもある）。
  (b) `<select>` の選択肢を `getByText` で判定したら、**Playwrightは `<option>` を
  hidden扱い**にするため「resolved to <option> — unexpected value hidden」で落ちた。
  どちらも機能は正常で、判定対象の選び方だけが原因（テスト直しの往復が1回ずつ無駄になる）。
- 検証: ページ内の導線は `a[href="…"]:not([data-nav-href])` のようにサイドバーを除外して掴む。
  `<select>` の中身は option の可視性でなく、周辺の見出し・ラベルや `toHaveValue` で判定する。
  新規テストは push 前に「判定対象が画面内で一意か（サイドバー・ヘッダに同名がないか）」を
  grep で確かめてから書く。
- 初出: 261 / 2026-08-25

## R-66: カスタマイズ可能な表示の設定UIは、定義順ではなく「実表示（カスタマイズ適用後）」を映す
- 分類: UI
- 背景: 251のメニュー名設定は nav-items.ts の静的定義順で項目を並べていたが、
  「ホーム」カテゴリは院長が並び替え・追加・削除できる（sidebar_home_items）。
  結果、設定画面とサイドバーの景色が食い違い、**目的のメニューを設定画面で探せない**
  という指摘になった（262）。設定UIは「何を設定できるか（定義）」ではなく
  「いま何が見えているか（実表示）」を正として並べる。カスタマイズの解決規則は
  正本1関数（`resolveHomeHrefs`）に置き、表示側と設定側が同じ関数を使う
  （2箇所に書くと片方だけ直して再びズレる）。実表示に無い項目（非表示中）は、
  実並びと混ざらないよう末尾に区切って出す（設定の口は失わせない）。
- 検証: 設定UIとその対象の実表示で、項目の並び（識別子の配列）が一致することを
  E2Eで機械判定する（C59が雛形: localStorage にカスタム並びを注入→両者の配列を比較）。
  解決規則は単体テストで固定する（U29）。
- 初出: 262 / 2026-08-25

## R-67: クライアント設定に依存する自動処理は、サーバー実行経路では「実行単位に設定値を焼き込んで」成立させる
- 分類: データ
- 背景: 263でバッチリサーチの自動保存（📚リサーチ保存）を設計した際、既存の自動ストック保存
  （247）の設定は localStorage にあり、サーバー（毎朝のcron）実行時には読めない。
  クライアント発火の保存に頼ると、**ブラウザが開いていない実行経路の分だけが静かに消える**。
  設定値はジョブ作成時に確定してジョブの列（auto_save_library・既定TRUE）へ載せ、
  サーバーはその値だけで保存を完結させる（設定変更は「その後に作るジョブ」から効く）。
- 検証: 自動実行（cron等）を持つ機能に自動保存・通知などを足すときは、その処理が
  サーバー側で完結するか（localStorage・画面stateを参照していないか）を確認する。
  E2Eで「作成時のフラグがジョブに確定する」ことを判定する（C60が雛形）。
- 初出: 263 / 2026-08-25

## R-68: 投稿先が決まっている生成物には、その媒体の記法・構造制約をプロンプトで課す（後の手直しに任せない）
- 分類: コンテンツ
- 背景: 261a のペルソナ別note記事は「note投稿前提」なのに、出力構造（タイトル分離・
  リード文・見出しの階層と本数・段落の空行）を課しておらず、院長から
  「大見出し・太字・改行がなく読みにくい。noteに投稿する前提で体裁も整えてほしい」と
  指摘された（264）。媒体が決まっているなら、その媒体で使える記法（noteはh2/h3の
  2階層・表やHTML不可）と貼り付けの運用（タイトルは別欄）まで含めて生成時に整える。
  見出し・タイトルは煽り・断定が最も出やすい箇所なので、本文と同じ医療広告ガードを
  **見出しにも明示的に**課す。
- 検証: 媒体前提の生成機能では (1) 出力構造・記法制約・可読性ルールがプロンプトにある
  (2) 別欄に貼るもの（タイトル等）が本文と分離して返る（分離はマーカー方式・
  欠落時は全文を本文に倒すfail-open） (3) 実AIで構造を検証する@genテストがある
  （B16が雛形: ##2本以上・h1なし・タイトル3本）。
- 初出: 264 / 2026-08-25

## R-69: 外部ナレッジを生成に注入するときは、品質ガードを必ず「後段・後勝ち」に置く
- 分類: コンテンツ
- 背景: 265でnote×X運用ナレッジ（拡散セオリー）を発信ハブの全生成経路に注入した。
  KBの推奨（常識の否定フック・数字の具体性・Before/After）は医療広告規制と衝突する箇所があり、
  順序を誤ると規約違反の文章が生成される。処理順は必ず〈ナレッジ適用 → 医療ガード適用 → 出力〉で、
  プロンプト上もナレッジの**後**にガードを置き、「衝突時はガードを優先する」と明文で宣言する。
  代表的な補正: 常識の否定は主語を「過去の自分の理解」に限定／Before/Afterの主語は自分（患者にしない）／
  数字は手順・時間・件数のみ（効果の数値化禁止）／「知らないと損」型の煽り禁止。
- 検証: ナレッジを注入する便では (1) プロンプト内でガードがナレッジより後にあること
  (2) 「ガード後勝ち」の宣言文があること (3) 生成物の禁止表現を機械検証する口があること
  （findBannedExpressions／validateXPost）。ナレッジはコード定数（getPlaybook・fail-closed）で持ち、
  ユーザーが編集できる置き場（AI参照素材）に入れない。
- 初出: 265 / 2026-08-26

## R-70: 媒体ごとに規則が異なる項目は、共有ロジックにせず媒体で分岐させる
- 分類: コンテンツ
- 背景: 265のKB v2で、ハッシュタグ規則が媒体で**真逆**（X=原則0個・最大1〜2個＝スパム判定回避／
  note=お題タグに積極参加＝公式ピックアップ狙い）、夜のゴールデンタイムもズレる
  （X=18:00〜21:00／note=20:00〜22:30）と明確化された。同一ロジックで共有すると
  誤った運用を促す。検証関数・時間帯の提示は媒体引数（media: 'x'|'note'）で分岐させ、
  X→note告知ではnote公開時刻とXポスト時刻を**別に**提示する。
- 検証: 媒体別の規則を扱う関数が media 引数を持ち、単体テストで両媒体の挙動差を固定する
  （U32が雛形: Xはハッシュタグ3個で警告・noteは警告しない）。
- 初出: 265 / 2026-08-26

## R-71: 表示用レンダラの変換を、貼り付け先が決まっているコピー経路にそのまま流用しない
- 分類: コンテンツ
- 背景: 266で院長の実地確認により「note用にコピーで貼ると大見出しが全て小見出しに落ちる」が判明。
  renderMarkdown は h1 を記事タイトルに予約して見出しを1段下げる（## → h3。**画面表示としては正しい**）が、
  リッチコピーがこの出力を流用していたため、note（h2=大見出し/h3=小見出し）では全見出しが1段沈んだ。
  264の見出し本数設計・265のKB適用が「貼った瞬間に無効化」されていた——生成・表示・E2Eが全部緑でも、
  **最終的な貼り付け先での見た目**は別の検証対象。貼り付け先が決まっているコピーは、
  その媒体のマッピングに合わせた変換（専用ラッパー）を通す。共有ヘルパー（copyRichMarkdown・
  Word体裁で53箇所実績）の既定動作は変えない。
- 検証: 媒体向けコピーを作る便では (1) 表示用レンダラの変換仕様（段ずらし等）を確認してから
  コピー経路を設計する (2) 変換は共有ヘルパーの既定を変えず専用ラッパーで行う（U34が雛形:
  ラッパーの変換と、共有ヘルパーの既定不変の両方を固定） (3) 可能なら実際の貼り付け先での
  見た目を院長実地確認の項目に含める。
- 初出: 266 / 2026-08-26

## R-72: 固定キャンバスへの描画は、折り返しうる全要素（タイトル含む）を行数で高さ見積もりに入れる
- 分類: UI
- 背景: 267で、まとめ画像（satori・固定キャンバス）のタイトルが2行に折り返すと下端が切れた。
  高さ見積もりは要点の折り返しは加味していたが**タイトルの折り返しが未算入**で、
  30字超のタイトル（実運用では普通に起きる）でキャンバスが不足していた。
  見積もりは「切れるより余る側」に倒し、さらに見積もりが外れた場合の保険として
  タイトル要素に flexShrink:0 を付けて圧縮させない（切れるのは余白側）。
- 検証: 見積もり関数の単体テストで「長いタイトルで高さが行数×行高ぶん増える」を固定し（U38）、
  E2Eで本番レンダリングのキャンバス高さが追随することを判定する（C64が雛形）。
  最小クランプの内側ではテストの差分が0に吸われるため、クランプ外のデータで比較する。
- 初出: 267 / 2026-08-26

## R-73: 内部タイムアウトは「リトライ回数×最悪所要＋バックオフ」で積算し、ルートのmaxDurationと整合させる
- 分類: データ
- 背景: 267で、GPT Image 2 の生成が本番で時間切れになった。1枚の最悪所要は約2分と
  コメントに書かれていたのに、内部タイムアウトは150秒＝**自動リトライ（最大2回・バックオフ込み）が
  1回でも起きると構造的に足りない**値だった。ルートの maxDuration も 180 で、
  同プロジェクトの慣行上限（300）より低く据え置かれていた。タイムアウトを決めるときは
  「1回の最悪所要 × (1+リトライ回数) ＋ バックオフ合計」で積算し、ルート上限の内側に余白を持って収める。
- 検証: withTimeout系の値を変える便では、リトライ実装（回数・バックオフ）を確認して積算根拠を報告に書く。
  タイムアウト文言は「時間切れであること・再試行できること・保存されていないこと」が伝わる形にする。
- 初出: 267 / 2026-08-26

## R-74: 「毎回同じであるべき出力」はAI生成にせず、決定的ロジックで導出する
- 分類: コンテンツ
- 背景: 268の収益化ロードマップは「タスクを淡々と実行できること」が目的であり、
  同じ状況で毎回違うタスクが出れば実行できない。フェーズ判定・週次タスク・通過条件・
  候補の並び（スコア）はすべて純関数で導出し（monetization-roadmap.ts）、
  AIは個別の提案（タイトル案・有料エリアの成果物案）に限定した。
  266の予約投稿カレンダー（AIなしで即時導出）と同じ設計で、判定・割り当て・並び替えの類は
  この方式を既定とする。あわせて、実行支援の出力には成果の断定（「必ず増える」等）を含めない
  （出すのは現在地・次にやること・通過条件のみ。注意書きを常時併記）。
- 検証: 判定・導出の関数が「同じ入力→同じ出力」を単体テストで固定していること（U39が雛形）。
  出力テキスト全量に断定文言が無いことを機械走査すること。
- 初出: 268 / 2026-08-26

## R-75: 表現・視点の変換で事実を変えない（変換系の生成には事実同一性の規約を必ず注入する）
- 分類: コンテンツ
- 背景: 269のKindle多軸展開は「章 × ペルソナ × 切り口」で表現を変えて書き下ろす機能で、
  簡略化・喩え・視点変更が**事実の変化に転ぶ**構造的リスクがある（「中学生でも分かる」向けに
  噛み砕いた結果、医学的に不正確になる等）。ペルソナ変換（261a）・テイスト変換（236）も同族。
  変換系の生成には〈元素材に無い医学的主張を追加しない／喩えから新たな結論を導かない／
  簡略化で因果関係を変えない／不確かなら書かない〉の事実同一性規約を、医療広告ガードとは
  **別枠で**必ず注入する。機械検証が困難な領域のため、元素材と並べた**目視確認UI**を必須で出す
  （確認の促し文言つき）。
- 検証: 変換系の生成プロンプトに事実同一性の規約ブロックがあること（FACT_FIDELITY_RULESの
  文言をU41で固定）。結果表示に元素材との並列表示と目視確認の促しがあること（C66が雛形）。
- 初出: 269 / 2026-08-26

## R-76: 取り消せる確認をまたぐ処理は、**取得に成功してから**破壊的操作を行う
- 分類: UI
- 背景: 270で📝テキスト分析に「📋 クリアして貼付」をiOSにも置くことになった。
  iOSは `clipboard.readText()` に必ず確認ポップアップを出し、**ユーザーはキャンセルできる**
  （R-61の既知の限界。回避手段は無い）。素直に「クリア → 読み取り → 貼付」と実装すると、
  キャンセルのたびに本文が消える——長文を書いた直後ほど損失が大きい。
  254は順序としては「先に読む」を守っていたが、**読めなかったときにクリアだけ実行する**
  分岐が残っていた（案A）。拒否が稀なデスクトップでは親切に見えても、確認が毎回出るiOSでは
  それが主要な経路になる。**確認をまたぐ操作は、キャンセルされる前提で設計する。**
  正しい順序と条件:
  (1) 先に取得する（readText / fetch / 選択ダイアログ）
  (2) **取得できて、中身があったときだけ**消す・置き換える
  (3) キャンセル・失敗・空 → **対象に一切触れない**（フォーカスも当てない。
      iOSでは focus() がキーボードを立ち上げて画面が動く）
  (4) どの結末でも黙って終わらせず、「消えていないこと」を最初に伝える（R-59）
  環境で分岐させないこと。デスクトップでも同じ順序にする——同じ操作が端末によって
  別の結果になる画面は説明できないし、失敗経路はデスクトップにも存在する（権限拒否・空）。
- 検証: 確認ポップアップはPlaywrightから観測できないため、**順序そのものを純関数で機械判定する**
  ——取得を失敗させたときに setText / backup が1回も呼ばれないことを固定する（U42が雛形）。
  E2Eは (a) 権限を与えないコンテキスト (b) 空クリップボード の2経路で入力が無傷であることを判定し、
  対象端末の実エンジン（iOSはWebKit）でも同じ判定を通す（C47・C46⑤・C52。R-64）。
  実機でしか確認できない範囲（確認ポップアップのキャンセル操作そのもの）は、
  **完了報告に「実機確認が必要な項目」として明記する**。
- 初出: 270 / 2026-08-26

## R-77: 「一括で変える」UIは、押した瞬間の適用だけでなく**以後の新規にも効く既定値**として持つ
- 分類: UI
- 背景: 272でバッチリサーチの文字数を一括変更できるようにした。既存行に配るだけの実装だと、
  そのあと「＋トピックを追加」した行が旧既定（263で決めた5000字）で入り、
  **押したはずの設定が効いていない**ように見える。一括操作は「今ある分を揃える」機能に見えるが、
  利用者の意図は「この画面ではこの値でいく」という宣言に近い。よって一括操作は
  〈既定値の更新〉＋〈既存分への適用〉の2つを必ず対にする。旧既定は**新しい既定値の初期値**として
  残し、消さない（既定を変えたのではなく、変えられるようにしただけ）。
  併せて、値の選択肢は配列1本を正本にし、行ごとのUIと一括UIの両方がそこから描かれる形にする
  （選択肢が増えたとき、片方だけ古いままにならない）。
- 検証: 一括操作を持つ画面のE2Eで「一括適用 → **そのあと新規追加した要素**が同じ値である」ことまで判定する
  （既存行が揃うことだけを見るテストは、この抜けを通してしまう。C68が雛形）。
  併せて、一括適用のあとに個別変更した要素が次の一括操作まで保たれることも固定する。
- 初出: 272 / 2026-08-27

## R-78: 横並び比較のUIは、比較対象が長文になった時点で「同期スクロール」と「見出しの固定」が必須要件になる
- 分類: UI
- 背景: 271でバッチリサーチ結果を最大3列で比較できるようにした。①ペルソナ別note記事の
  読み比べ（冒頭サンプル＝短文）と同じカードUIで足りると考えたが、扱うのが3,000〜5,000字に
  なった瞬間に2つ破綻する——(1) 列ごとに独立スクロールだと、片方を送った時点で対応関係が
  失われて「比較」でなくなる (2) 送るとトピック名が画面外へ出て、どの列が何か分からなくなる。
  短文では顕在化せず、長文では**必ず**踏む。よって横並び比較を作るときは、対象の長さを先に
  見積もり、長文なら〈割合ベースの同期スクロール（OFFにできる）〉と〈列ヘッダーのsticky〉を
  最初から要件に入れる。同期はピクセルでなく**割合**で合わせる（長さの違う列で破綻するため）。
  列数も「入るだけ並べる」ではなく、1列あたりの読める幅（日本語で35〜40字/行）から上限を決める。
- 検証: 比較UIのE2Eで (a) 1列を送ると他列も動く (b) トグルでOFFにすると動かない
  (c) ヘッダーの computed position が sticky で、スクロール後も列の上端に居る——の3点を判定する
  （C69が雛形）。列数・同期位置の計算は純関数に出し、長さの違う列で対応することを単体で固定する（U43）。
- 初出: 271 / 2026-08-27

## R-79: 保存済みデータを解釈する処理のテスト入力は、**書き込む側のコード**から写す（記憶で組み立てない）
- 分類: E2E
- 背景: 271で「保存済み要約セクションの取り出し」を単体テスト化した際、fixtureの見出しを
  `## 📋 要約` ＋ 次行に `（1000字以内）` と書いた。実際に書き込む側（batch-research の run route）は
  `## 📋 要約（1000字以内）` と1行で組み立てており、パーサの「先頭行を捨てる」処理の対象がずれて
  テストだけが落ちた。**パーサ側は正しく、テストの入力が嘘だった**。この取り違えは、実装が
  正しいのにテストを合わせて緩める方向へ倒れやすく、危険度が高い。
- 検証: 保存済みテキストを解釈するテストを書くときは、生成側の該当行を grep して
  （例: `grep -rn "## 📋 要約" src/app/api`）**その文字列を写す**。テストが落ちたら、
  まず「入力が実データと同じ形か」を疑ってからパーサを直す。
- 初出: 271 / 2026-08-27
