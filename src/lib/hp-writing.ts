// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// HP文章の執筆設計 一元管理（184）
// HP内容自動生成（/dashboard/hp-generator）は患者が読む公開情報＝医療広告ガイドラインの
// 遵守が最も厳格に求められる出力。執筆強化（コピーライティング/心理学）と禁止事項を
// 定数で一元管理し、生成プロンプトと医療広告ガード（/api/hp-guard）の両方から参照する。
// 183の note-writing.ts と同じ思想。調整はこのファイルの変更だけで完結させる。
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// 執筆強化：患者に届く文章のためのコピーライティング／心理学（医療の節度の範囲で）
export const HP_WRITING_DESIGN = `# 執筆設計（読み手に届く文章のための指示）
- 読み手の不安・疑問を先に言語化して安心させる（「こんなことでお悩みではありませんか」の類型は可。不安を煽るのは不可）
- 専門用語には必ず平易な言い換えを添える
- 一文を短く。主語は読み手に置く（「当社は/当院は〜」より「あなたは〜できます」寄り）
- 具体（症状・場面）→ 説明 → 次の一歩 の順で書く
- 見出しだけで内容が分かるようにする（流し読み耐性）
- 来訪・利用のハードルを下げる情報は事実ベースで（初めての流れ・所要時間・準備物など）`;

// 医療広告の観点での禁止事項（生成プロンプトに明示。医療・ヘルスケア以外の業種でも安全側に倒す）
export const HP_AD_PROHIBITED = `# 禁止事項（医療広告ガイドライン準拠・全業種で安全側に倒す）
- 効果効能の断定（「必ず治る」「〜が改善します」等）
- 他者との優位性比較・最上級（「地域No.1」「最高の」等。根拠のない誇大表現全般）
- ビフォーアフター表現・体験談（患者の感想の掲載は規制対象）
- 不安を煽る表現（「放置すると危険」「手遅れになる前に」等）
- 限定性・希少性の演出（「今だけ」「先着」等）
- 費用に関する誤認を招く表現（総額が分からない表示・割引誘導等）
- 数値・統計を新たに作らない（入力情報に書かれている数値の転記のみ可）
- 出典の捏造禁止（入力情報に無い研究名・実績・数値を創作しない）`;

// 医療広告ガード（/api/hp-guard）の検出観点は、186で8観点に統一した共通定数
// lib/medical-ad-check.ts の MEDICAL_AD_NG_RULES を直接参照する（二重管理しない）。
// ※184で一時的にここへ置いた HP_AD_CHECK_RULES は186で廃止（共通側へ統合済み）。
